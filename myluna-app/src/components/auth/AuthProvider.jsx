// This is the same as src/context/AuthContext.jsx
// You can create a re-export or just use the context directly
// Create this file as a re-export:

import { AuthProvider } from '../../context/AuthContext';
export { AuthProvider };